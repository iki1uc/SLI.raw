export function AXIOM(frame) {

    // Axiom 1 – Scan
    const scan = 0;

    // Axiom 2 – Operator
    const operator = 0;

    // Axiom 3 – Pipeline
    const pipeline = KItriKIme(frame) === 1 ? 0 : 0;

    return { scan, operator, pipeline };
}
